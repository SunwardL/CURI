# CURI：Codex Usage & Reliability Insights 项目计划

**文档日期：** 2026-09-25  
**状态：** 初步方案，适合做 MVP 前的范围确认  
**暂定项目名：** CURI — Codex Usage & Reliability Insights（Codex 使用量与可靠性洞察）

## 1. 项目概述

计划把本地 API 中继与 Codex 使用监控整合成一个本地运行的开发者工具。主界面同时展示服务可靠性和个人用量：请求是否成功、失败后重试了什么、当前额度窗口、今日 token 与调用情况、历史每日趋势，以及可观测的请求模型差异。

方案以 [Steady Relay](https://github.com/937204197/steady-relay) 作为请求转发与安全重试组件，以 [Codex Model Watch](https://github.com/ysh1112/codex-model-watch) 的本地会话日志解析和监控能力作为观察组件。新增工作的核心是统一两边的事件数据，并在一个本地页面中呈现。

**主要用户：** 使用 Codex CLI，并通过本地代理、第三方 OpenAI 兼容服务或自定义上游访问模型的开发者。直接使用 ChatGPT 登录且未设置自定义 API Base URL 的用户，需要单独确认代理接入方式。

## 2. 参考项目分析

### 2.1 Steady Relay

- 项目：[937204197/steady-relay](https://github.com/937204197/steady-relay)
- 实现文件：[main.go](https://github.com/937204197/steady-relay/blob/main/main.go)、[proxy.py](https://github.com/937204197/steady-relay/blob/main/proxy.py)
- 许可证：README 标注 MIT。

**功能：** 本地运行的 OpenAI 兼容 API 代理。客户端把 API Base URL 指向本地代理，代理再把请求转发到用户明确配置的上游。遇到连接错误、超时和部分临时 HTTP/SSE 错误时，代理可按退避策略重试。它会识别 SSE 中的心跳、元数据、输出和终止事件，避免在已向客户端提交实际输出后重放请求。

**已有诊断信息：** 当前 Go 实现会在终端记录请求模型、请求 ID、上游错误、重试次数、总耗时、首字节时间和流结束状态，并提供健康检查地址。可选的 `buffer-until-success` 模式会等到成功终态后再放行流式响应，以便处理较晚出现的失败；代价是增加首字节等待时间和缓冲内存。

**与整合项目相关的缺口：** 检查的代码中尚未看到结构化的请求事件文件或 `/metrics`、`/stats` 接口；SSE 诊断能读取错误与事件类型，但尚未看到提取响应中的实际模型名。把这些字段以版本化 JSONL 输出，是监控侧稳定接入的切入点。

**实现取舍：** 项目以 Go 版作为主要代理实现；仓库提供 Python 标准库备用实现，但 README 说明增强的流式缓冲模式仅 Go 版支持。为了复用现有流式重试行为，首版建议保留代理为独立进程，由 Python 监控层负责启动辅助、读取事件和展示数据。

### 2.2 Codex Model Watch

- 项目：[ysh1112/codex-model-watch](https://github.com/ysh1112/codex-model-watch)
- 主实现：[codex_model_watch.py](https://github.com/ysh1112/codex-model-watch/blob/main/codex_model_watch.py)
- 许可证：README 标注 MIT。

**功能：** 纯 Python 标准库的本地监控工具。它增量读取 `~/.codex/sessions` 中的 JSONL 会话日志，展示实际模型、token 用量、额度窗口、容量错误、项目分布和请求耗时，并提供本地网页面板。现有代码解析 `token_usage_record`、`turn_context`、`task_complete` 和 `token_count.rate_limits`，能够作为新增用量看板的数据基础。

**主动探针：** 项目还会使用本机 Codex 登录态请求 `chatgpt.com/backend-api/codex/responses`，从返回的 SSE 元数据读取探测时实际返回的模型。它可以观测某一时刻的请求模型与响应模型关系。README 指出历史会话日志不能恢复被改派前的原始请求模型，因此探针与历史统计解决的是不同问题。

**边界：** 当前探针依赖本地 `auth.json` 和 ChatGPT Codex 后端接口。这个接口不是通用 OpenAI 兼容上游接口；README 也将 rollout 日志格式列为内部格式，并提示可能随 Codex 版本变化。因此，接入第三方上游时需要 provider adapter，不能假设现有探针可直接通用。主动探针只代表探测时刻，不代表全天路由状态，也不能单独证明回答质量下降。5 小时和周限额应按最新日志中的 `rate_limits` 窗口动态展示；若某个账户或计划没有提供对应窗口，就显示“不可用/暂无数据”和数据时间，不显示为 0，也不根据计划名称硬编码额度。

## 3. 整合价值与产品定位

两个项目处在请求链的不同位置：Relay 观察请求和转发过程，Model Watch 观察 Codex 会话与账户用量。整合后可以把“可用性、路由、用量”放进同一时间线：

```text
Codex ──请求──> Steady Relay ──转发──> 配置的模型上游
                    │
                    └──本地请求事件──┐
                                     ├──> Python 监控层 ──> 本地实时看板
Codex 会话 JSONL ──> 用量/工具事件 ──┘       │
                                      可选的上游主动探针
```

建议将产品描述为**Codex 本地请求可靠性与模型路由监控**。首版解决服务错误、重试、延迟、额度和可观测的模型差异；质量评测作为后续可选能力。

## 4. 首版功能范围

### 4.1 首版包含

1. **当前额度卡片**：展示本机最新记录到的 5 小时与周窗口使用百分比、剩余额度（若源数据能计算）和重置时间。窗口名称与可用状态取自记录；Pro 等计划若没有 5 小时窗口，显示“此账户当前未提供”，不显示 0。
2. **今日使用量**：按用户本地时区汇总今日输入、输出、缓存 token；reasoning token 只在源日志可识别时单列。显示会话数、轮数、可识别的模型调用数和工具调用数。
3. **每日历史**：对可读取的本地会话日志按日聚合 token、轮数、工具调用数和错误数，提供趋势图、模型/项目筛选与覆盖日期范围。默认读取 `sessions`；归档会话作为可配置数据源，并在界面标出是否纳入。
4. **实时工具活动**：从新追加的会话事件更新工具调用计数，按 shell、MCP、浏览器/搜索和其他类别汇总。按一次工具调用计数一次，不把对应的工具结果事件再算一次；Relay 的上游重试次数单独展示。
5. **额度与历史的刷新说明**：监控层增量读取本地 JSONL，并在新事件到达后更新今日计数；额度只在 Codex 写入新的 `rate_limits` 快照后更新。界面显示“最后更新时间”，将这种状态描述为准实时，避免没有新事件时误示为实时额度。
6. **Relay 事件接入**：新增本地 JSONL 事件输出，至少记录时间、请求 ID、请求模型、HTTP 状态、错误类别、重试次数、首字节耗时、总耗时和流结束状态。
7. **模型差异展示**：若上游响应明确报告模型名，记录为 `reported_model` 并与 `requested_model` 比较；未报告时显示“未知”，不猜测。
8. **本地仪表盘**：汇总额度、今日使用量、每日历史、工具活动、请求成功率、容量错误、重试、延迟、模型分布和可观测模型差异；仅监听回环地址。
9. **配置与诊断命令**：提供 `doctor` 检查 Codex Home、会话目录、归档路径、Relay 地址和事件文件是否可读；提供清晰的上游配置说明。
10. **隐私默认值**：只保存用量与请求元数据，不保存 prompt、完整请求体、响应正文或 API Key；不上传日志或遥测。

**计数口径：** “轮数”“模型调用数”“工具调用数”和“Relay 重试次数”是不同指标。若本地格式只能稳定识别轮数，界面就展示轮数；只有识别到单次模型生成/调用事件后，才单列模型调用数。工具调用按唯一调用事件计数，并保留未知工具类别，避免把不能识别的记录静默丢掉。

### 4.2 首版暂缓

- 自动切换多个上游、账号池或模型路由策略。
- 通过回答主观判断“模型降智”。
- 自动读取、复制或同步 API Key 与 `auth.json` 凭据。
- 云端账户、团队协作或远程日志服务。
- 对所有第三方服务承诺模型替换检测。

## 5. 建议的事件格式

Relay 与 Python 监控层通过本地 JSONL 文件交换请求事件；Codex 会话用量与工具活动仍从本机 rollout JSONL 增量解析。字段可按版本演进：

```json
{
  "schema_version": 1,
  "timestamp": "2026-09-25T12:00:00Z",
  "request_id": "req-000001",
  "requested_model": "model-a",
  "reported_model": "model-a",
  "status": 200,
  "attempts": 2,
  "first_byte_ms": 420,
  "duration_ms": 3800,
  "error_class": null,
  "stream_terminal": "response.completed"
}
```

`reported_model` 仅在上游响应实际包含且成功解析时填写。Codex 会话 JSONL 还会形成独立的用量事件，字段包括 `timestamp`、`session_id`、`turn_id`、`project`、`input_tokens`、`cached_tokens`、`output_tokens`、可选 `reasoning_tokens`、`event_kind` 和可选 `tool_name`。原始 prompt 与工具参数不进入看板数据库。若 Codex 会话和 Relay 请求之间没有共同的 thread/request ID，首版只做时间与模型维度的并列趋势，不将其伪装成精确的逐会话关联。

缺失值必须保留为 `null`/“暂无数据”。额度值、token 值或调用数没有记录，不等于数值为 0。历史汇总需在页面标出日志覆盖起止日期、纳入的会话目录和最近扫描时间。

## 6. 分阶段实施

### 阶段 A：整合原型

- 阅读两个项目的许可证和接口，记录可复用组件及版本。
- 以 Codex Model Watch 的本地面板和增量日志解析作为监控基础，先实现额度卡片、今日用量卡片和每日历史趋势。
- 为用量解析增加工具调用事件识别和分类；区分会话、轮数、模型调用、工具调用与 Relay 重试，记录来源覆盖范围和最后扫描时间。
- 让会话 JSONL 新事件到达时更新今日指标；展示 5 小时/周额度快照的更新时间，并对缺失或过期快照给出明确状态。
- 为 Steady Relay 增加可选的 JSONL 诊断事件输出；保留现有终端日志和健康检查行为。
- 做出同屏的额度、今日使用量、历史趋势、工具活动、Relay 状态、最近请求、重试和容量错误视图。

### 阶段 B：模型路由可观测性

- 在不改写转发响应的前提下，解析 SSE 中可用的模型字段。
- 增加请求模型与响应报告模型的差异记录、筛选和告警。
- 用 `unknown / matched / different` 三态展示结果，并保留上游未提供模型名时的未知状态。

### 阶段 C：探针与质量金丝雀

- 定义 provider adapter 接口，分开支持 ChatGPT Codex 登录态与用户配置的 OpenAI 兼容上游。
- 主动探针由用户显式开启，配置执行频率、模型、提示词和额度预算。
- 可选运行固定小型基准任务，比较输出质量变化；页面应单独标记这是抽样评估，而非路由事实。

**粗略开发估算：** 若复用现有 Go Relay 与 Python Watch，阶段 A 可按 1–2 周个人开发估算；阶段 B、C 取决于 SSE 格式和 provider 适配情况，建议分别评估后再排期。

## 7. 主要技术与产品风险

| 风险                             | 影响                                    | 应对方式                                                     |
| -------------------------------- | --------------------------------------- | ------------------------------------------------------------ |
| Codex rollout JSONL 是内部格式   | Codex 更新后日志解析可能失效            | 解析器做版本容错；无效事件跳过并在 `doctor` 中提示           |
| ChatGPT 后端探针接口变化         | 现有 Model Watch 探针可能失效           | 把探针实现封装为 provider adapter；通用上游探针独立实现      |
| 上游不返回真实模型字段           | 无法比较请求模型与实际模型              | 显示未知；探针或供应商元数据可补充证据                       |
| Relay 重试改变调用语义           | 多次执行请求可能造成重复副作用          | 延续只在安全提交点前重试的规则；保留可选完整缓冲模式         |
| 事件无法关联到 Codex 会话        | 不能做精确的会话级分析                  | 先按时间、模型和请求 ID 展示；有可靠公共标识后再关联         |
| 本地日志包含敏感信息             | 用户隐私和凭据风险                      | 不记录正文或密钥；默认回环监听；导出前提供脱敏预览           |
| 额度窗口缺失或快照过期           | 看板可能把未知额度误报为 0 或当前实时值 | 按源数据动态显示可用窗口；缺失显示“暂无数据”，同时显示最后更新时间，不按计划名称推断 |
| 会话日志不完整或归档目录未纳入   | 每日 token/调用总量可能低于实际值       | 展示纳入的目录、可读取日期范围、最近扫描时间和归档是否启用   |
| 不同事件口径造成调用重复统计     | 今日调用数和工具数失真                  | 分开展示轮数、模型调用、工具调用和 Relay 重试；工具只按调用事件计一次，不按结果事件重复计数 |
| Codex 事件结构变化或工具类型未知 | 新版本事件可能漏计或无法分类            | 保留未知事件/工具类别计数，并在诊断信息中提示解析覆盖情况    |
| “模型名变化”被误解成质量下降     | 产生不准确结论                          | 将路由差异与质量评测分开，文案只描述可观测字段               |

## 8. 首版验收目标

- 用户能通过一个本地命令启动或检查监控组件，并确认代理与 Codex 会话数据是否接通。
- 仪表盘能呈现最新可用的 5 小时和周额度窗口；窗口缺失时显示“暂无数据/此账户当前未提供”及最后更新时间，而不是 0。
- 今日卡片能汇总可读取日志中的输入、输出、缓存 token，以及轮数、可识别的模型调用和工具调用；每日历史展示趋势及数据覆盖范围。
- 工具调用按调用事件计数一次，工具结果和 Relay 上游重试不会重复计入 Codex 工具调用数。
- 仪表盘能同时呈现 Codex 用量和 Relay 请求可靠性数据，并标明额度快照与本地事件的更新时间。
- 对每个请求能查看请求模型、尝试次数、延迟、状态和错误类别；未获得实际模型名时明确显示未知。
- 默认运行不上传数据，也不记录完整 prompt、响应正文或密钥。
- Relay 的既有转发和安全重试行为保持可用；缓冲模式的额外等待和内存占用有清晰说明。

## 9. 授权与代码复用

两个参考仓库的 README 均标注 MIT。若直接复用代码，保留各自版权、许可证与来源说明，并清楚标明新项目和原项目的关系。更稳妥的首版集成方式是保持 Relay 与监控层模块边界，通过版本化 JSONL 事件接口协作；这样可以分别更新代理和监控组件。

## 10. 参考链接

- [Steady Relay 仓库](https://github.com/937204197/steady-relay)
- [Steady Relay README](https://github.com/937204197/steady-relay/blob/main/README.md)
- [Steady Relay Go 实现](https://github.com/937204197/steady-relay/blob/main/main.go)
- [Codex Model Watch 仓库](https://github.com/ysh1112/codex-model-watch)
- [Codex Model Watch README](https://github.com/ysh1112/codex-model-watch/blob/main/README.md)
- [Codex Model Watch Python 实现](https://github.com/ysh1112/codex-model-watch/blob/main/codex_model_watch.py)

---

**结论：** 组合方向成立。首版价值来自把 Codex 的额度窗口、今日与历史用量、工具活动，以及 Relay 的请求重试诊断放进统一的本地看板，并补上结构化事件与有条件的模型响应比对。额度和历史指标都以本机可读取的数据为准，明确标记缺失值与数据覆盖范围。先做好个人用量看板和本地事件整合，再扩展 provider 探针和质量金丝雀，可以把工程范围控制在可发布的开发者工具规模内。